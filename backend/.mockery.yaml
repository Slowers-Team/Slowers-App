with-expecter: true
packages:
    github.com/Slowers-team/Slowers-App/databases/mongo:
        config:
            dir: "mocks"
            mockname: "{{.InterfaceName}}"
            outpkg: "mocks"
        interfaces:
            Database:
